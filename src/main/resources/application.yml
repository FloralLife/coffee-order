server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:40036/coffee-order?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: yunolee
    password: yunolee@1234
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
